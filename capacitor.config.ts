import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.netspie.jpconversations',
  appName: 'Japanese Conversations',
  webDir: 'dist'
};

export default config;
